# Linear Integration — Substance2Remix

## Overview
Registered as a project under the shared **RTX Remix Plugins** team.

## Key Context
- Substance Painter plugin — Python API + RTX Remix REST API
- Labels: `push`, `pull`, `auth`, `textures`, `blender-unwrap`, `api-compat`
- API compat check runs every Monday (see `api-compat-check.yml`)

## Commands
```bash
python linear/sync.py --status
python linear/sync.py --push
python linear/sync.py --blockers
```
